ensure kb files are in the same directory as the python script, works better with ebooks, update 'fileList.conf'. Using the command prompt in the SynthReason directory type 'dir /b *.txt > fileList.conf' to auto generate the configuration file
