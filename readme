ensure kb files are in the same directory as the python script, update 'fileList.conf' if needed. Using the command prompt in the SynthReason directory type 'dir /b *.txt > fileList.conf' to auto generate the configuration file, use qpython for "AGI speech.py", place files in correct directory...
